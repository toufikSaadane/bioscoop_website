@model website.Models.Enquete

@{
    ViewData["Title"] = "Create";
}

<div class="container" style="margin-top:5rem;">
    <h2 style="text-align:center; margin-bottom:25px;">Enquete invullen</h2>
    <hr />
    <div class="container" style="width:35rem; margin-top:25px">
        <div class="row">
            <div class="col-md-12">
                <form asp-action="Create">
                    <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                    <div class="form-group">
                        <label asp-for="QuestionText" class="control-label">Naam</label>
                        <input asp-for="QuestionText" class="form-control" />
                        <span asp-validation-for="QuestionText" class="text-danger"></span>
                    </div>
                    <div class="form-group">
                        <button type="submit" value="Create" class="btn btn-dark">Versturen</button>
                    </div>
                </form>
            </div>
        </div>
        </div>
    </div>

    @section Scripts {
        @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
    }
